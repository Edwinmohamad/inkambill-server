function pageInfo(req,defaultSize=50,maxSize=100){const page=Math.max(1,Number.parseInt(req.query.page,10)||1);const requested=Number.parseInt(req.query.per_page,10)||defaultSize;const perPage=Math.min(maxSize,Math.max(10,requested));return {page,perPage,offset:(page-1)*perPage};}
async function paginate(db,sql,params,req,defaultSize=50){const p=pageInfo(req,defaultSize);const [[count]]=await db.execute(`SELECT COUNT(*) total FROM (${sql}) paged_source`,params);const total=Number(count.total||0),pages=Math.max(1,Math.ceil(total/p.perPage)),page=Math.min(p.page,pages),offset=(page-1)*p.perPage;const [rows]=await db.execute(`${sql} LIMIT ? OFFSET ?`,[...params,p.perPage,offset]);return {rows,pagination:{page,perPage:p.perPage,total,pages,from:total?offset+1:0,to:Math.min(offset+p.perPage,total)}};}
module.exports={pageInfo,paginate};
